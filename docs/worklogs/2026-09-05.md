# 2026-09-05 工作紀錄

## 已完成

- 將預設查詞卡改為「本句判斷 → 詞義 → 發音與重音 → 搭配 → 例句 → 換你說」的學習順序。
  `發音與重音` 取代整串 KK 音標，要求模型標主重音並只給一個可操作的發音技巧。
- 強化 `S`：兩行內保留條件、否定、程度、語氣與責任歸屬；有歧義才說明一個原句線索。
- 強化 `D`：以原句證據解釋一個卡點，並只帶走一個可重用的表達、用法與不同情境例句。
- 修正查詞三選一的規格：正解必須是原句唯一成立的意思，干擾項反映合理誤讀；不得把原句
  未保證的內容寫成必然事實。範例同步改正。
- 已將舊版三個未自訂預設 prompt 納入遷移；使用者自訂 prompt 保留。
- YouTube 最小整合已完成：native caption DOM 查詞、`A`／`S`／`D` 暫停影片、收藏來源 URL
  加 `t` 與可讀時間、卡片快捷鍵隔離播放器，以及 fullscreen mount；重用既有 DB、AI、語境與 FSRS。
- 版本升為 `1.8.5`，於 2026-09-05/06 建立 ZIP：
  [wordtiger-1.8.5-chrome.zip](/home/ubuntu/vocab-ext/.output/wordtiger-1.8.5-chrome.zip)。

## 決策與研究結論

- 先優化 YouTube，Netflix 與沉浸式翻譯後排；待 YouTube 實測完成後再決定是否另開整合。
- 現有的 A／S／D、AI 請求、收藏和 FSRS 可以重用；新增複雜度集中在字幕來源、字幕斷句、
  暫停／恢復、全螢幕與快捷鍵衝突。
- 不直接假設字幕 DOM 或 `video.textTracks` 可用。沉浸式翻譯可負責雙語字幕，攔詞虎聚焦英文
  字幕的查詞、理解、收藏與複習，避免重做翻譯。

## 驗證

- `pnpm test`：20 個測試檔、373 項測試全部通過。
- `pnpm typecheck`：通過。
- `pnpm build`：通過。

## 限制

- 使用者回報 YouTube 可用滑鼠指向字幕後按 `A` 叫出查詞卡；原先 `Space` 會同時觸發播放器
  播放，已納入快捷鍵隔離情境。
- 目前自動驗證以 jsdom 與 fixture 為主，尚未宣稱真實瀏覽器中的字幕 DOM、播放器快捷鍵、
  fullscreen 顯示或時間戳回跳已完成驗證。
- Netflix 的 `A` 尚未成功，依決策暫停排查。字幕抓不到時的原因提示、手動選字或貼上句子備援
  尚未實作。

## 後續

- 在可用瀏覽器環境手動驗證 YouTube 英文字幕、A／S／D 暫停、播放器快捷鍵隔離、fullscreen
  顯示與帶 `t` URL 的回跳。
- 依手動結果抽查 A／S／D 的模型回覆，必要時調整 prompt；再評估字幕抓不到提示與手動備援。
