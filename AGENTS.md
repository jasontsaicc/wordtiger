# 專案協作慣例

## 版本與封裝

使用者於 2026-09-20 指定：以後專案有變動，自動推進版本並打包，不必逐次詢問。

- 每批修改完成時升版一次，不是每改一個檔案就升版；使用者另有指示時優先遵循。
- 一般修改預設遞增 `package.json` 的 patch 版本，例如 `1.8.10` → `1.8.11`。同批工作若已升版，不重複遞增。
- 更新 `CHANGELOG.md`，記錄日期與本次變更。
- 執行適合本次修改的檢查，通過後執行 `pnpm zip`，產生最新 Chrome／Edge MV3 封裝。
- 驗證 ZIP 完整性及其中 `manifest.json` 的版本與 `package.json` 一致；檢查失敗時修正後再封裝，不把失敗產物報為完成。
- 保留舊版 ZIP，不提交 `.output/` 到 Git；完成時提供版本號、最新 ZIP 路徑與驗證結果。
- 自動封裝不等於自動送審或發佈到瀏覽器商店。

本慣例從下一批專案修改開始套用；本次僅記錄指示，不另升版。
