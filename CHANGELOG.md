# 變更紀錄

## 未發佈：上架準備

- 介紹站改用原生 Node 建置與獨立檢查，支援 Cloudflare Pages Git 整合，不需安裝擴充功能依賴。

- 設定頁加入三步開始、AI 連線測試與離線使用指南；同步收合為進階選項。
- 修正隱私政策的 AI 語境、觸發操作、同步資料與保留說明。
- 新增一頁式介紹站，共用使用指南與產生的隱私頁；尚未部署或送審。

## 1.8.9 - 2026-09-08

- 查詞 A 卡片補強白話文法說明，恢復「字族與構詞」並用白話解釋常見後綴（例如 `-ity`）；英文定義固定兩行，搭配最多兩組，例句與「換你說」各保留一組可帶走的內容。

## 1.8.8 - 2026-09-07

- 「原形 / 構詞」改名成「原形 / 這次怎麼變」，固定三行：原形、這次、原因。原因用白話說為什麼這句會變成這個字形，過去分詞、副詞這類術語只放在括號裡補充。原本的 `occurred → occur（過去式 / 過去分詞）` 要先懂詞性才看得懂，等於把成本推給讀的人。
- 箭頭方向統一成由基礎往衍生，例如 `progress → progressive`，跟學習順序一致。單純的字形變化不再用箭頭，改用三行標籤。
- 「本句意思」收短成英英定義加一行「詞性：最短中文詞組」，不再重述原句情境。原本它和「本句用法」會把同一件事講兩次。

## 1.8.7 - 2026-09-07

- 查詞卡新增「原形 / 構詞」「本句意思」與「語感與使用情境」，把形式、句中義與實際使用場合分開說明。
- 原形與構詞只在有學習價值時顯示，補上相關中文意思；保留原有發音、搭配、例句與「換你說」練習。

## 1.8.6 - 2026-09-07

### 修正

- 三選一的出處原句被切在字中間，殘句反而讓人猜不出意思。原本以被查的字為中心、前後各留 40 個字元，切點不看單字邊界，所以 `API` 印成 `I`、`consecutive` 印成 `cons`。原句改成整句照出：句長本來就在取句階段收在 300 字內，而三個選項按鈕在卡片捲動區之外，長句只會在內文捲動，推不掉按鈕。

### 調整

- 原句只有代名詞時，補上同段落的前一句。`It was throttled until the window reset.` 這種句子不看上文就不知道 `It` 指誰，那不是題目難，是無解。前一句用較安靜的樣式放在原句上方；段落第一句與影片字幕沒有上文，該行整個不出現。這行資料取句時本來就算好了，只有查詞這條路把它丟掉，所以不多抓資料也不多問 AI。

## 1.8.5 - 2026-09-05

- YouTube 原生字幕支援暫停影片、帶播放時間收藏，並隔離卡片快捷鍵與播放器快捷鍵。
- 全螢幕時保留卡片顯示；Netflix 支援後排。

## 1.8.4 - 2026-09-05

- A 查詞以「發音與重音」取代 KK 音標，補一個具體發音技巧、原句判斷線索、用法框架與口頭造句練習；修正三選一範例的語意過度推論。
- S 保留兩行速讀，強調條件、否定與語氣；D 用原句證據解釋卡點並教一個可重用表達。
- 三卡舊預設自動遷移，自訂提示詞保留。

## 1.8.3 - 2026-09-01

### 修正

- 三選一在 1.8.2 幾乎不會出現，三個原因各自都足以讓整題消失，全部修掉。
- 1.8.1 之前留下的、以及同步下來的詞典快取永遠出不了題。這些列沒有 prompt 指紋，命中判斷把「欄位缺少」當成相符，所以改了 prompt 也不會重查，整份舊詞庫都停在沒有題目的那份答案上。現在開著三選一又抽不出題目時會自動重問一次，重問後的列帶著指紋，每個字最多多花一次 AI 呼叫。
- 模型照著格式說明寫，題目就作廢。原本的規格寫成「答案｜正確選項是第幾個(1-3)」，模型把這句說明也抄進答案行，變成「答案｜正確選項是第1個」；選項行的「選項｜」字首同樣被當成欄位說明而省略。規格改成一個填好的範例，抽取端同時放寬：答案行取第一個 1-3 的數字，選項行沒有字首時由下一行的答案行認出來。
- 格式跑掉的題目會漏到卡片上。剝除只認有字首的選項行，抽取失敗時那兩行就原樣印出來，答案在作答之前就先被看到。現在剝除與抽取認同一個形狀。

### 調整

- 題目上方放出處原句，被查的字標粗，太長就以那個字為中心前後各留 40 字。只給三個中文選項不給句子，等於要人憑空猜。
- 題目文案從「選一個最接近的」改成邀請你猜，並說明為什麼要先猜；提示行同步改成「1／2／3 猜一個」。
- 揭曉的對錯獨立成卡片的一個區塊，不再接在詞典本文前面。答對是琥珀色，答錯是紅色並寫出你猜的那一個，略過維持灰色。只講正解的話，你回頭看不到自己錯在哪。
- 字義單純的字不再出湊數的題目。原本兩條出題規則都有前提，兩條都不成立時規格是空的，模型只能硬湊一個明顯錯誤的干擾項。題型改成依序判斷的三條，最後一條是邊界題：三個選項意思相近，只差範圍、強度或方向，例如「減輕影響，但沒有解決」對上「完全消除影響」。
- 選項一律用中文。曾經考慮用英文定義當選項，但辨別力來自干擾項的範圍與方向對比，換成中文一個都沒少；英文只在你剛承認不認識這個字的那一秒，多收 20 幾個字的閱讀成本。
- 詞典本文的「詞性與釋義」每一條先寫英文定義、下一行才是中文。定義要用比被查的字更簡單的字、12 個英文字以內，寫不出來就只寫中文。英文放在揭曉之後，不擋在作答前面。

### 升級須知

- 查詞的 prompt 又改了一次，所以 1.8.2 建立的查詞快取全部失效，每個字下次查會重新問一次 AI。

## 1.8.2 - 2026-09-01

### 新增

- 查詞時先答一題三選一，猜完才揭曉完整詞典。按 `A` 之後三個選項會跟第一批 token 一起到，詞典在後面繼續串，等待的那幾秒換成一次真正的提取。用 `1`／`2`／`3` 作答，或用 `A` 跳過。
- 揭曉的第一行直接講對錯與正解，其下就是原本那張詞典卡。答錯卻還沒看到原因的中間狀態換不到任何東西，所以矯正回饋緊貼著錯誤發生的那一刻。
- 每次作答記進新的 `quizLog`，含選項內容。日後可以回頭看哪些題目沒有鑑別度，例如某個字每次都秒答。
- 設定頁加上「查詞先猜再揭曉」開關，預設開啟。它只管顯示與作答，不改 prompt，所以開關切換不會讓任何已經查過的字失效。

### 調整

- 打老虎的題目會換句子。同一個字如果在不同頁面查過好幾次，每輪複習會輪到不同的那句，依收藏順序由舊到新。以前不管複習幾次都配同一句，等於在背那一句而不是在練那個字。
- 出題那句跟 AI 產生詞典時看到的那句不同時，揭曉後會標出來。詞典內文的 **本句** 指的是後者，不標的話兩句會對不起來。

### 修正

- 同步過的新裝置不再掉語境。詞典的來源句只存在本機、不上雲，所以舊版在新裝置同步完之後找不到語境，單字題會退化成「你記得這個字的核心意思嗎？」。新的輪替邏輯不再依賴那個欄位。
- 片語不再在「片語」和「句型」之間跳。判斷要用「說意思」還是「造句」出題，原本看當下顯示的那句裡有沒有字面的片語，`roll back` 這種字如果某一句存的是 `rolled back` 就會來回跳，連評分按鈕的字都跟著變。改成看該片語的全部語境。

### 升級須知

- **雲端同步要先在 Supabase SQL Editor 重跑一次 `supabase/schema.sql`**，建立 `quiz_log` 資料表與 RLS 政策。這段可重複執行，既有資料不會遺失。
- 沒有重跑就更新擴充套件的話，**整個同步都會停**，不是只有三選一不能用。同步的任何一張表拉取失敗就會中斷整輪，所以單字、語境、詞典快取與打老虎成績都會一起停在原地。重跑 SQL 之後會自動恢復，資料不會遺失。
- 查詞的 prompt 改了，所以既有的查詞快取全部失效，每個字下次查會重新問一次 AI。這是拿快取換題目，不是錯誤。
- 已經同步下來的舊詞典沒有題目，在那台裝置上會維持沒有三選一，直到你對那個字按一次卡片右上角的 ↻ 重問。

## 1.8.1 - 2026-08-30

### 新增

- 卡片右上角加上「重試」。AI 的回答有隨機性，同一個字、同一份 prompt 也可能這次答得不好；按 ↻ 會跳過快取重問一次。查詞、快速看懂、拆懂這句三張卡都有。
- 重試只重新問 AI，不會再記一次收藏語境。收藏記的是「你遇到這個字」，重問一次答案並沒有再遇到一次。
- 重問失敗時保留原本那份答案，錯誤訊息放進卡片底部的提示列，不會因為想要更好的而失去已經有的。

### 調整

- 卡片被拖走之後留在你拖到的位置，只有換一個字才回到單字旁邊。原本任何一次重新載入都會把卡片彈回去。

## 1.8.0 - 2026-08-29

### 新增

- 「我的攔路虎」改成收合列：一行講完遇到幾次、練過幾次、抓到幾次，點開才載入 AI 詞典與來源語境。
- 總表每列顯示「遇到 N 次」，數的是保留下來的語境筆數。同一句在同一頁重複收藏只算一次。
- 總表加上五個學習進度分類（全部、今天排到、還沒練過、排程中、已馴服）。
- 總表加上單字／片語篩選，片語那列另外掛一顆「片語」徽章。它跟學習進度是兩個獨立的軸，可以同時生效。
- 展開總表的列後可以按「重查」，先清掉這個字的快取再重問一次 AI。
- 工具列 popup 加上詞頻門檻拉桿，不用進設定頁就能調整整篇文章亮多少字。

### 調整

- 「今晚打老虎」的間隔改由 FSRS 決定，不再是固定的 1、3、7、14、30 天。同一疊卡片裡，常溜掉的字會更快回來，早就記住的字可以排到 30 天以上。
- 關掉 FSRS 官方的 1 分鐘與 10 分鐘 learning steps。同一張卡不會在同一晚再次出現。
- 下次複習日對齊當地午夜。晚上十點多練完排到「明天」的卡，隔天白天打開就選得到。
- 一輪的選題順序改成：先出已到期的卡（依日期由早到晚），再用還沒練過的新卡（依收藏日）補足。
- 一輪 5 題，答完一題直接進下一題，整輪答完才出現收工畫面。題與題之間不再插入「今天又前進 1 步」。
- 練習畫面的單位改成題：「本輪 5 題」、「第 3 題／共 5 題」、「今天已練 N 題」。一輪算抓到一隻。
- 練習與總表都不再顯示下次複習日期。自評後仍回傳下次時間，但只當成寫入成功的訊號。日期不顯示之後，「下次複習」排序一併移除。
- 1.8.0 之前用舊階梯練過的字，總表寫「升級後排程重新開始」，不再跟從沒練過的字共用「還沒練過」。那些字有練習紀錄卻沒有 FSRS 卡片，兩句話混用會自相矛盾。
- 「已經馴服」改成間隔排到 30 天以上才出現。停在舊第 5 階的卡片仍然保留這個按鈕。
- 總表的「已馴服」不再把按 `X` 排除的字算進去，那些字標示為「已排除」。
- 沒有 AI 詞典的字在總表標成「還不能出題」，不會掛在「今天排到」卻永遠不出題。總表的數字跟實際題數一致。
- 展開總表的列不再自動送出查詞請求，改成按「查一次詞」才花 AI 額度。
- 頁面卡片改成單一重點色：拆法與例句這類參考資料用灰色細線分層，卡點與帶走才點亮琥珀。查詞卡的段落標題跟著同一套。深色模式的錯誤訊息也改成讀得到的顏色。
- 高亮詞頻門檻的預設值從 5,000 名改成 10,000 名，新安裝一打開文章不會整片亮起來。拉桿下限一併改成 2,000 名，讓刻度對齊每格 2,000。
- 「學習足跡」改名「老虎足跡」。

### 移除

- 拿掉「今天先到這裡」。整輪答完才收工，中途想走直接切分頁，已作答的成績不會掉。
- 拿掉設定頁的「AI 回答庫」分頁。換模型或改 prompt 時，快取本來就會因為 prompt 指紋改變而自動重查，這一頁的用途已經由程式處理掉；想重問單一個字改用總表的「重查」。

### 升級須知

- 這一版需要在 Supabase 重新執行 `supabase/schema.sql`，`words` 會加上 `fsrs_card` 欄位。沒有更新 schema 時本機學習照常運作，只有同步會顯示錯誤。
- 既有卡片一律視為 FSRS 新卡，不繼承舊階梯進度，升級後第一次的間隔會比以前短。練習次數與抓到次數的歷史紀錄不受影響。
- 舊的 `reviewStep`、`reviewDueAt` 欄位保留但不再更新，也不再參與排程。
- 詞頻門檻的新預設只影響全新安裝。已經動過設定的裝置會保留自己存下來的值，要改請自己拉一次拉桿。

## 1.7.0 - 2026-08-28

### 新增

- 查詞卡同時顯示實際字形與原形，例如 `slammed → slam`。AI 也一起收到兩者與來源句，能以 `get/be slammed (with + noun)` 這種完整搭配教學，不再孤立解釋原形。
- `F` 朗讀頁面上看到的實際字形，不再只念資料庫裡的原形。
- 查詞 prompt 新增 `{{surface}}` 變數，可在設定頁自行使用。

### 調整

- 查詞快取改為記錄實際字形、來源句與 prompt 版本。同一個字換一個句子會重新查詢，改過 prompt 也會自然失效。
- 同步下來或舊版留下的快取缺少這些欄位時視為命中，不會在第二台裝置上重查一次。
- 「拆懂這句」的回答改成「意思、拆法、卡點、帶走」的閱讀階層，卡片依標籤呈現層次，支援深色與高對比模式。
- 快取答案頁不再傳送它用不到的 prompt 全文，每一列少 2.7 KB。
- 舊版預設 prompt 的升級改用完全比對。自己改寫過的 prompt 一律原樣保留。

### 升級須知

- 這一版不需要重跑 `supabase/schema.sql`，雲端資料表沒有變動。
- 實際字形、來源句與 prompt 版本只留在本機，不會上傳到 Supabase。

### 修正

- `notes`、`nodes`、`codes`、`modes`、`routes` 不再被還原成 `not`、`nod`、`cod`、`mod`、`rout`。舊規則對所有 `-es` 結尾一律多刪一個字母，但英文只有嘶音結尾的複數才會補 `e`。
- `morning` 不再變成 `morn`，`programming` 不再變成 `programme`，`hissing` 不再變成 `his`。
- `constructor`、`toString` 這類在程式文件裡常見的字不再讀到 JavaScript 內建物件，卡片標題與查詞不會再壞掉。
- 按 `X` 標成已認得之後再按 `Space` 收藏，卡片不再是空白的。
- 從舊版升級時，沒有改過的預設 prompt 現在會正確更新。先前只有部分歷史版本會被認出來，其餘的人會永遠停在舊 prompt。
- 詞庫的 AI 詞典在查詢失敗時改為顯示已經同步下來的定義，不再只給錯誤訊息。沒設 API key 的裝置也看得到雲端來的解釋。

## 1.6.0 - 2026-08-27

### 新增

- 學習足跡月曆併入打老虎紀錄：每格顯示當天收藏與練習次數，點日期可看練了哪些字、各抓到幾次。
- 統計卡新增「本月練習次數」與「本月抓到」。
- 「我的攔路虎」每張卡顯示該字的累計練習次數。

### 調整

- 打老虎的逐次成績存進新的 `reviewLog` 資料表，並加入 Supabase 同步，換裝置也看得到同一份練習紀錄。
- 成績只新增不修改，因此用 uuid 當主鍵、不設 tombstone，同步時直接覆蓋同一個 id。
- 「已經馴服」改由 background 的 `masterWord` 一次完成記錄與改狀態，UI 只在真的成功時才顯示成功。
- 匯出 JSON 補上 `reviewLog`。
- 單字新增 `collectedAt` 欄位記錄收藏事件。學習足跡改看這個欄位，不再用目前的 `status` 反推歷史。

### 升級須知

- 雲端同步要先在 Supabase SQL Editor 重跑一次 `supabase/schema.sql`，建立 `review_log` 資料表與 RLS 政策。
- 同一份 SQL 會把 `contexts` 與 `review_log` 的主鍵換成 `(user_id, id)`，並為 `words` 補上 `collected_at`。這段可重複執行，既有資料不會遺失。
- 首次開啟時會依現況回填 `collectedAt`：目前是生詞、留過語境或練習過的字視為收藏過，其餘視為按 `X` 排除。「先按 `X`、日後才收藏」的舊資料會被算在按 `X` 那天，因為真正的收藏日從來沒有被記錄過。
- `schema.sql` 會用同一套規則回填雲端的 `words.collected_at`，全新裝置才不會拉到一片空白的歷史。這段只填 `null` 且判定為收藏過的列，重複執行不會再動到任何資料。
- 舊版沒有保留複習紀錄，月曆的練習資料從本版開始累積。

### 修正

- 按 `X` 標成已認得的字不再出現在學習足跡的「新收藏」。舊版每按一次就會新增一筆當天收藏紀錄，等於愈排除愈多。
- 收藏後標成「已馴服」的字不會再從「新收藏」消失，該筆歷史留在原本收藏那天。
- 先按 `X` 排除、日後才收藏的字不再記到按 `X` 那天。`createdAt` 是建立列的時間，不是收藏時間。
- 同步時的收藏日改成取兩邊非空值中較晚的一個，跟「重新收藏取最新日期」一致。舊寫法交給 `updatedAt` 決定勝負，兩個方向都會掉資料：遠端的 null 會蓋掉本機剛回填的值，本機的 null 也會把雲端已有的收藏日推成 null。
- 修正切換 Supabase 帳號後同步會永久失敗：`contexts` 與 `review_log` 的主鍵只有 `id`，重新上傳同一個 uuid 會撞到舊帳號受 RLS 保護的列。
- 自評與「已經馴服」的兩筆寫入改成同一個 Dexie transaction。舊寫法在第二筆失敗時會只推進複習間隔，卻沒留下紀錄。

## 1.5.0 - 2026-08-26

### 新增

- 發音改用 OpenAI `gpt-4o-mini-tts` 與 `marin` AI 聲線，以親切自然、容易跟讀模仿的美式英文朗讀。
- 「今晚打老虎」的片語與句型題可朗讀完整來源句。
- 快速看懂與拆句卡片可按 `F` 朗讀完整原句。

### 調整

- 單字卡按 `F` 仍朗讀單字；介面明確標示 AI 語音。
- AI 語音未設定、相容端點不支援或播放失敗時，自動退回裝置內建英文語音。
- API key 維持只由 background 使用，不傳入 content script。

### 修正

- 改用 Web Audio 播放 AI 語音，避免非同步 API 回應完成後遭瀏覽器阻擋自動播放。
- 連續朗讀時取消前一筆未完成的 TTS 請求與播放，避免重疊與多餘用量。

## 1.4.0 - 2026-08-26

### 新增

- 加入全站小虎懸浮球、固定開發版 ID 與單站自動標示。
- 加入學習足跡月曆、來源文章整理與「今晚打老虎」間隔複習。
- 改善非母語學習者的片語／句型教學，並建立可同步的片語 AI 詞典。

### 修正

- 修正複習候選飢餓、語境與答案錯配、片語題型不自然，以及查詞時未累積新語境等問題。
